<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-dark bg-dark p-3">
        <a class="navbar-brand" href="#">Admin Dashboard</a>
        <a href="{{ path('admin_logout') }}" class="btn btn-danger">Déconnexion</a>
    </nav>

    <div class="container mt-5">
        <h1 class="text-center">Bienvenue, {{ user }}</h1>
        <p class="text-center">Vous êtes connecté en tant qu'administrateur.</p>

        <div class="d-flex justify-content-center mt-4">
            <a href="#" class="btn btn-success me-2">Gérer les utilisateurs</a>
            <a href="#" class="btn btn-info">Voir les statistiques</a>
        </div>
    </div>
</body>
</html>
